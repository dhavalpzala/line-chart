import { SERVER_URL } from '../config';

const uploadData = (data) => {
  // uncomment this code to send data to server

  // return fetch(SERVER_URL + 'upload', {
  //   method: 'post',
  //   headers: { 'Content-Type':'application/json' },
  //   body: JSON.stringify(data)
  // });
}

export { uploadData }
